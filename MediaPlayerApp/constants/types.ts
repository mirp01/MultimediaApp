export interface AudioFile {
    id: number;
    title: string;
    file_path: string;
    duration?: string;
}

export interface ImageFile {
    id: number;
    title: string;
    file_path: string;
}